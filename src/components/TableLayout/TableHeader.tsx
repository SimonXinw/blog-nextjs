import React from 'react';
import ContentContainer from '@/components/ContentContainer';

function TableBodyCopy(props: any) {
  return <ContentContainer style={{ padding: 20 }} {...props} />;
}

export default TableBodyCopy;
