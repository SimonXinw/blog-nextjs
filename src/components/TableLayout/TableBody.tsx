import React from 'react';
import ContentContainer from '@/components/ContentContainer';

function TableBody(props: any) {
  return <ContentContainer style={{ padding: '20px' }} {...props} />;
}

export default TableBody;
